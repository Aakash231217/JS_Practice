const user ={
    firstName:'harshit',
}

console.log(user.firstName);
//console.log(user.address.houseNumber);//error
console.log(user?.address?.houseNumber);//? will check whether it is there or not or in future will be created